<%= form_with(model: enrollment) do |form| %>
  <% if enrollment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
        <% enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= collection_select(:enrollment, :student_id, Student.all, :id, :name) %>
  </div>

  <div>
    <%= form.label :institution_id, style: "display: block" %>
    <%= collection_select(:enrollment, :institution_id, Institution.all, :id, :name) %>
  </div>

  <div>
    <%= form.label :course_price, style: "display: block" %>
    <%= form.text_field :course_price %>
  </div>

  <div>
    <%= form.label :number_payments, style: "display: block" %>
    <%= form.number_field :number_payments %>
  </div>

  <div>
    <%= form.label :due_day, style: "display: block" %>
    <%= form.number_field :due_day %>
  </div>

  <div>
    <%= form.label :course_name, style: "display: block" %>
    <%= form.text_field :course_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
